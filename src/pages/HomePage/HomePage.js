import { useGetUserQuery } from 'states/apiSlice';
import styles from './HomePage.module.css'

function HomePage(props) {
  // const data = useGetUserQuery()
  // console.log(data);

  return (
    <div>
      this is home page
    </div>
  );
}

export default HomePage;